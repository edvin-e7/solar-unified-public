# debug-error.md

[Meta-prompt content abstracted for IP protection]

This is a meta-prompt — a prompt that drives self-modification of other
prompts in the autonomous learning loop (see `backend/autonomous_learner.py`).
The exact wording is kept private; the architecture pattern is publicly
documented in the README and PRD.

## Replace before use

Drop in your own meta-prompt text following the calling convention in
`backend/autonomous_learner.py`.

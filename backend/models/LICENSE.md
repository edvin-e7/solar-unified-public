# Model weights license

The runtime code (`backend/services/detection_model.py`,
`backend/services/detection_geom.py`) is part of Solar Unified and uses the
project's primary license.

The ONNX weights file (`yolov8n-solar-seg.onnx`) is **not** part of this
repository. Whoever places the file here is responsible for honoring its
upstream license. Record the actual license + source URL of the weights
you used here once downloaded:

```
Source:   <url>
License:  <SPDX id, e.g. AGPL-3.0 or MIT>
SHA-256:  <hash of the .onnx file>
Date:     <YYYY-MM-DD>
```
